/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ManageProfileButtonComponent } from './manage-profile-button.component';

describe('Component: ManageProfileButton', () => {
  it('should create an instance', () => {
    let component = new ManageProfileButtonComponent();
    expect(component).toBeTruthy();
  });
});
