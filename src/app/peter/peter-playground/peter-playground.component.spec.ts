/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PeterPlaygroundComponent } from './peter-playground.component';

describe('Component: PeterPlayground', () => {
  it('should create an instance', () => {
    let component = new PeterPlaygroundComponent();
    expect(component).toBeTruthy();
  });
});
